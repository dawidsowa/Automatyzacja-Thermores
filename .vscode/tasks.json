{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "pandoc",
      "type": "shell",
      "command": "pandoc ${fileDirname}/${fileBasename} -o ${fileDirname}/${fileBasenameNoExtension}.html && xdg-open ${fileDirname}/${fileBasenameNoExtension}.html",
      "problemMatcher": []
    }
  ]
}
